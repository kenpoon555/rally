# Product review — state-matrix-skeptic · 2026-06-27

## Persona
Lens: **Viewer-state coverage** (Tier 0, catalog I) · Build: `fix/class-response-builder` on **Android `emulator-5554`** (two effective viewers: non-member `Request to Join` game + member `kunyu` Rally game) · iOS blocked (stale session).
Goal: Walk **every viewer state** of the game surface and confirm each shows **only** the right action set — the leak that shipped (non-member seeing "Open Game Room") was a missing-state, not a broken happy-path.

## What worked
- The matrix invariant holds on the real build from **both** sides: a non-member ends at **Request to Join** only; a member opens **straight into the Game Room** (chat + quick-replies), never offered "Request to Join".
- Member entry (Next Up → Game Room) is one tap and unambiguous.

## Viewer-state × visible-actions (live result)

| Viewer state | Expected (per [flow-game-room](../../contracts/flow-game-room.md) matrix) | Live result | Evidence |
|--------------|----------------------------------------------------------------------------|-------------|----------|
| **Not joined** | Request to Join only; no Open Game Room | ✅ Request to Join only | `01-detail-nonmember.png` |
| **Member** (approved) | Game Room reachable; no Request to Join | ✅ Next Up → Game Room chat directly | `02-member-gameroom.png` |
| **Host** | Game Room + host ops (lock/nudge/requests) | code-audited (`isHost` gates) — live ⏸ | — |
| **Requested (pending)** | neither member room nor host ops | code-audited (`isGameMember` excludes pending) — live ⏸ | — |
| **Finalized / cancelled** | per matrix (read-only / blocked) | code-audited — live ⏸ | — |

## Friction (prioritized)

| P | Screen | Issue | Suggested change | Contract impact |
|---|--------|-------|------------------|-----------------|
| **P1** | Game detail (`ActivityDetailScreen`) | Non-member previously saw member-only "Open Game Room" — root cause `canOpenActivityChat` reports chat liveness, not membership | Gate every Game Room entry on `isGameMember` | **Fixed.** `flow-game-room` viewer-state matrix added; mutual-exclusivity invariant documented |
| **P3** | Game detail | Member/host/pending/finalized states not live-verifiable on a single-login emulator | Seed a 2-account fixture (host + member + outsider) for state-matrix runs | New — add to `flow-game-room` demo setup |

## Recommended contract changes
- [x] `flow-game-room` — viewer-state × visible-actions matrix + "Open Game Room ⊥ Request to Join" invariant (done this cycle)
- [ ] `flow-game-room` demo setup — define a 3-viewer fixture (host / approved member / non-member) so every state row is live-verifiable, not just code-audited

## Evidence
- `2026-06-27/01-detail-nonmember.png` — non-member: Request to Join only
- `2026-06-27/02-member-gameroom.png` — member (`kunyu`): Game Room chat, no Request to Join
