# Product review — coach-approved-manual · 2026-06-22 (tier 2 picky)

## Persona

**Role:** R1→R2 — Coach candidate → approved coach  
**Level:** Fresh adult, no coach seed  
**Goal:** Complete before/after manual founder approval (`is_coach` flip); confirm Coach Tools unlock per v1 beta flow without founder hand-holding beyond contract SQL step.  
**Contract:** [flow-become-a-coach.md](../../contracts/flow-become-a-coach.md)  
**Queue:** `onboarding-round2-picky` · tier 2

## Setup

| Item | Value |
|------|-------|
| Device | iPhone 16 sim (`06244EDD-C6DC-4A80-92A2-ADC1D73B9382`) |
| App | Native dev client `com.rallyapp` (Expo Go insufficient — Firebase native modules) |
| Flags | `EXPO_PUBLIC_ENABLE_COACH_FOUNDATION=true`, `EXPO_PUBLIC_ENABLE_PARENT_STUDENT_CORE=true`, `EXPO_PUBLIC_ENABLE_PARENT_PILOT=true` |
| Account | `@playerr0474532` (`player.r0.1782081474532@rally-mvrhoops.demo`) — reused R0 account, `is_coach` reset to `false` pre-run |
| Approval SQL | `update public.profiles set is_coach = true where email = 'player.r0.1782081474532@rally-mvrhoops.demo';` |
| Refresh | Force-quit + relaunch (`simctl terminate` → `launch`) |

---

## Before state (`is_coach = false`)

**DB:** `is_coach=false`

| Surface | Expected | Observed |
|---------|----------|----------|
| Profile → Settings | No Coach Tools | **Pass** — FAMILY + PAYMENTS/SCHEDULE; no COACH TOOLS section |
| Play → Create game | Pickup only, no Class/Clinic | **Pass** — standard Create Game form, no Class/Clinic banner |
| Today | No CLASSES I TEACH | **Pass** — no coach dashboard card |

Screenshots: `2026-06-22/01-before-play-create-pickup-only.png`, `02-before-profile-settings-no-coach-tools.png`, `03-before-today-no-classes-i-teach.png`

---

## Approval step (v1 manual)

Founder runs linked DB update (simulated):

```sql
update public.profiles
set is_coach = true
where email = 'player.r0.1782081474532@rally-mvrhoops.demo';
```

**Result:** `is_coach=true` returned for `playerr0474532`.

No in-app apply form exists (expected v1 stub). Reviewer force-quit and reopened app per contract.

---

## After state (`is_coach = true`, post-relaunch)

| Surface | Expected | Observed |
|---------|----------|----------|
| Profile → Settings | Coach Tools section | **Pass** — COACH TOOLS with Coach Profile, Payment link, Class Templates, **Create Class** |
| Coach Tools → Create Class | Navigates to class create | **Pass** — Create Game with **Class / Clinic** banner (“Coach-only create flow…”) |
| Today | Coach dashboard card | **Pass** — **CLASSES I TEACH** + empty-state copy |
| Today parent block | No misleading MY CLASSES for zero-child coach | **Pass** — MY CLASSES block not shown (B4 fix holds) |
| No v2 promises | No substitute/academy UI | **Pass** |

Screenshots: `2026-06-22/04-after-profile-settings-coach-tools.png`, `05-after-create-class-entry.png`, `06-after-today-classes-i-teach.png`

---

## Contract checklist (v1)

| # | Item | Result |
|---|------|--------|
| 1 | Non-coach adult: no Coach Tools | **Pass** (before) |
| 2 | Non-coach: no coach-only Create Class | **Pass** (before) |
| 3 | After `is_coach=true`: Coach Tools without reinstall | **Pass** (after relaunch) |
| 4 | Create Class → class create flow | **Pass** |
| 5 | No substitute/academy UI text | **Pass** |
| 6 | Non-Marcus reviewer (no hardcode mask) | **Pass** — unlock tied to DB flag |

---

## What worked

- Full **before → SQL flip → relaunch → after** path completes without founder UI intervention beyond documented SQL.
- Coach Tools discoverable on Profile Settings after relaunch; **Create Class** and Today **CLASSES I TEACH** give two entry points.
- Today no longer shows parent **MY CLASSES** empty state for this zero-child newly approved coach (improvement vs tier 1).
- Login + permission gates (location, notifications) are skippable; no silent failures on the coach unlock journey itself.

## Friction (prioritized)

| P | Screen | Issue | Suggested change | Contract impact |
|---|--------|-------|------------------|-----------------|
| P1 | Process | **No in-app apply** — founder must run SQL/dashboard; reviewer has no self-serve path | v2: Profile “Become a coach” + admin queue (contract stub) | None for v1 — document in pilot onboarding |
| P2 | Process | **Requires full app relaunch** — Coach Tools / Today card do not appear until force-quit + reopen | v2: realtime profile subscription or “Check status” after apply | Add relaunch requirement to TestFlight reviewer notes |
| P3 | Create Class | Lands on shared **Create Game** screen with Class/Clinic banner only — easy to miss mode switch | Dedicated “Create Class” header or tab defaulting to class fields | Minor — note in `flow-create-game` |
| P3 | Profile → Settings | VALIDATOR dev rows visible under coach settings — noise for persona review | Gate behind `__DEV__` only | None |
| P3 | Profile → Settings | FAMILY section still visible for approved coach with zero children | Consider hiding Family block when `studentCount === 0` and user is coach-only | Cross-ref parent personas |

**Tier 2 silent-failure check:** No silent failures observed on this persona path. Login automation is fragile (keyboard focus shifts field coordinates) but app login works when credentials are entered correctly.

## Sport-specific (if seed mismatch)

- Account onboarded to **badminton**; coach unlock is role-based, not sport-seeded. No sport mismatch impact.

## Recommended contract changes

- [ ] `flow-become-a-coach`: Add TestFlight reviewer script block (login → confirm no tools → SQL → relaunch → screenshot checklist).
- [ ] `flow-become-a-coach`: Note Today **CLASSES I TEACH** as alternate entry (not only Profile).
- [ ] `flow-become-a-coach`: Document relaunch as required v1 step (not optional).

## Comparison to tier 1 (2026-06-21)

| Item | Tier 1 | Tier 2 picky |
|------|--------|--------------|
| Before/after unlock | Pass | **Pass** (re-verified live) |
| MY CLASSES on Today for coach | P2 friction | **Resolved** — not shown |
| Relaunch required | P2 | **Still P2** — unchanged |
| No in-app apply | P1 / v2 | **Still v2** — unchanged |
