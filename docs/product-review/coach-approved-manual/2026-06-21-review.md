# Product review — coach-approved-manual · 2026-06-21

## Persona

**Role:** R1→R2 — Coach candidate → approved coach  
**Level:** Fresh adult, no coach seed  
**Goal:** Document before/after manual founder approval (`is_coach` flip); confirm Coach Tools unlock per v1 beta flow.  
**Contract:** [flow-become-a-coach.md](../contracts/flow-become-a-coach.md)

## Setup

| Item | Value |
|------|-------|
| Device | iPhone 16 sim (`06244EDD-C6DC-4A80-92A2-ADC1D73B9382`) |
| Flags | `EXPO_PUBLIC_ENABLE_COACH_FOUNDATION=true` |
| Account | `@playerr0474532` (`player.r0.1782081474532@rally-mvrhoops.demo`) — reused from prior R0 review, no coach/parent seeds |
| Approval SQL | `update public.profiles set is_coach = true where email = 'player.r0.1782081474532@rally-mvrhoops.demo';` |
| Refresh | Force-quit + relaunch (`simctl terminate` → `launch`) |

---

## Before state (`is_coach = false`)

**DB:** `is_coach=false`, `children=0`

| Surface | Expected | Observed |
|---------|----------|----------|
| Profile → Settings | No Coach Tools | **Pass** — no COACH TOOLS section (PAYMENTS, SCHEDULE, … only) |
| Profile → Me | No coach entries | **Pass** |
| Play → Create game | Pickup only, no Class/Clinic | **Pass** — standard game form, no role picker |
| Today | No CLASSES I TEACH | **Pass** — MY CLASSES parent block only |

Screenshots: `2026-06-21/01-before-profile-me-no-coach-tools.png`, `02-before-profile-settings-no-coach-tools.png`, `03-before-play-create-pickup-only.png`

---

## Approval step (v1 manual)

Founder runs linked DB update (simulated):

```sql
update public.profiles
set is_coach = true
where email = 'player.r0.1782081474532@rally-mvrhoops.demo';
```

**Result:** `is_coach=true` returned for `playerr0474532`.

No in-app apply form exists (expected v1 stub). Reviewer force-quit and reopened app per contract.

---

## After state (`is_coach = true`, post-relaunch)

| Surface | Expected | Observed |
|---------|----------|----------|
| Profile → Settings | Coach Tools section | **Pass** — COACH TOOLS with Coach Profile, Payment link, Class Templates, **Create Class** |
| Coach Tools → Create Class | Navigates to class create | **Pass** — opens Create Game with **Class / Clinic** banner (“Coach-only create flow…”) |
| Play → Create (via coach path) | Class option available | **Pass** — same Class/Clinic entry visible after approval |
| Today | Coach dashboard card | **Pass** — **CLASSES I TEACH** + “Create your first class →” |
| No v2 promises | No substitute/academy UI | **Pass** |

Screenshots: `04-after-profile-settings-coach-tools.png`, `05-after-create-class-entry.png`, `06-after-today-classes-i-teach.png`

---

## Contract checklist (v1)

| # | Item | Result |
|---|------|--------|
| 1 | Non-coach adult: no Coach Tools | **Pass** (before) |
| 2 | Non-coach: no coach-only Create Class | **Pass** (before) |
| 3 | After `is_coach=true`: Coach Tools without reinstall | **Pass** (after relaunch) |
| 4 | Create Class → class create flow | **Pass** |
| 5 | No substitute/academy UI text | **Pass** |
| 6 | Non-Marcus reviewer (no hardcode mask) | **Pass** — unlock tied to DB flag on fresh account |

---

## What worked

- Clean **before → SQL flip → relaunch → after** unlock path matches contract north-star.
- Coach Tools section is discoverable immediately on Profile Settings after relaunch.
- Create Class and Today **CLASSES I TEACH** give two entry points — reasonable for a new coach.
- No false “Become a coach” CTA that would confuse v1 manual-approval model (correct absence).

## Friction (prioritized)

| P | Screen | Issue | Suggested change | Contract impact |
|---|--------|-------|------------------|-----------------|
| P1 | Process | **No in-app apply** — founder must run SQL/dashboard; reviewer has no self-serve path | v2: Profile “Become a coach” + admin queue (already stubbed in contract) | None for v1 — document in pilot onboarding |
| P2 | Process | **Requires full app relaunch** — no pull-to-refresh or profile reload after approval | v2: realtime profile subscription or “Check status” after apply | Add relaunch requirement explicitly to TestFlight reviewer notes |
| P2 | Today | **MY CLASSES** (parent copy) still shown alongside **CLASSES I TEACH** for coach with zero children | Hide parent block when `studentCount === 0`; show coach block only | Cross-ref `player-no-coach-tools` review |
| P3 | Create Class | Lands on shared **Create Game** screen with Class/Clinic banner — workable but easy to miss that mode switched | Dedicated “Create Class” header or tab defaulting to class fields | Minor — note in `flow-create-game` |
| P3 | Profile → Settings | VALIDATOR dev rows visible (class enroll picker) — noise for persona review | Gate behind `__DEV__` only | None |

## Sport-specific (if seed mismatch)

- Account onboarded to **badminton**; coach unlock is role-based, not sport-seeded. No sport mismatch impact.

## Recommended contract changes

- [ ] `flow-become-a-coach`: Add TestFlight reviewer script block (signup → confirm no tools → SQL → relaunch → screenshot checklist).
- [ ] `flow-become-a-coach`: Note Today **CLASSES I TEACH** as alternate entry (not only Profile).
- [ ] `flow-become-a-coach`: Cross-link Today MY CLASSES visibility rule from `player-no-coach-tools` friction.
