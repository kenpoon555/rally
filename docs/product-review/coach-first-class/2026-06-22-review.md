# Product review — coach-first-class · 2026-06-22 (tier 2 picky)

## Persona

**Role:** R2 — Approved coach  
**Level:** Approved coach creates first class and shares **parent enrollment** invite  
**Goal:** Coach Tools → Create Class → publish → share parent invite (not host pickup link)  
**Contract:** [flow-create-game.md](../../contracts/flow-create-game.md) · [flow-student-class-enrollment.md](../../contracts/flow-student-class-enrollment.md)  
**Queue:** `onboarding-round2-picky` · tier 2

## Setup

| Item | Value |
|------|-------|
| Device | iPhone 16 sim (`06244EDD-C6DC-4A80-92A2-ADC1D73B9382`) |
| App | Native dev client `com.rallyapp` |
| Flags | `EXPO_PUBLIC_ENABLE_COACH_FOUNDATION=true`, `EXPO_PUBLIC_ENABLE_PARENT_STUDENT_CORE=true`, `EXPO_PUBLIC_ENABLE_PARENT_PILOT=true` |
| Primary account | `@playerr0474532` — approved coach (`is_coach=true` from persona 2 SQL) |
| Reference account | `@marcus` — demo class listings only (document north-star share CTA) |

---

## Journey summary (`@playerr0474532`)

| Step | Contract expectation | Observed | Result |
|------|---------------------|----------|--------|
| Profile → Coach Tools → **Create Class** | Navigates to class create | **Pass** — COACH TOOLS + Create Class row | **Pass** |
| Create Class form | Class/clinic mode, class-oriented publish | **Partial** — Class/Clinic banner shown; header still **Create Game**; CTA **Publish game** | **Partial** |
| Publish class | Creates coach class listing + parent enroll path | **Fail** — creates **pickup activity**; detail shows **Pickup game** + **Copy link** (host invite) | **Fail** |
| Today **CLASSES I TEACH** | New class appears | **Fail** — card still empty (“Create your first class”); created activity not listed as class | **Fail** |
| **Share parent enrollment invite** | Class detail → parent `class-enroll` link | **Not reachable** — no `ClassDetail` for non-Marcus coach after create | **Fail** |

---

## Reference path (`@marcus` — demo data)

| Step | Observed | Result |
|------|----------|--------|
| Today → **CLASSES I TEACH** → Youth Basketball Clinic | Demo class rows with Roster / Message Parents | **Pass** (demo only) |
| Class detail → Overview | **Share parent enrollment invite** button visible | **Pass** (north-star UX exists on demo listings) |

**Note:** `listCoachClasses` returns `DEMO_COACH_CLASSES` filtered by `coach_user_id === MARCUS_ID` only. Fresh approved coaches do not get class listings from **Create Class** publish.

---

## What worked

- **Create Class entry** from Coach Tools is discoverable for approved non-Marcus coach.
- **Class / Clinic banner** on create form signals coach-only intent (same as persona 2).
- **Publish** succeeds without silent failure — lands on game detail with share affordances.
- **Marcus demo** proves **Share parent enrollment invite** UX exists on `ClassDetailScreen` when a `CoachClassListing` is present.

## Friction (prioritized)

| P | Screen | Issue | Suggested change | Contract impact |
|---|--------|-------|------------------|-----------------|
| P1 | Create Class → Publish | **`createMode: 'class'` still calls `createActivity` pickup path** — no coach class listing, no parent enroll invite; coach gets host **Copy link** instead | Wire class publish → `coach_class_listings` + navigate to `ClassDetail` with `ensureClassInvite` | **Fails** coach-first-class north-star for non-Marcus coaches |
| P1 | Today **CLASSES I TEACH** | Empty after publish; demo classes only for Marcus user id | Persist created classes per coach; remove Marcus-only demo filter for real creates | `flow-coach-onboarding-org` |
| P2 | Post-create detail | Card labeled **Pickup game** despite entering via Create Class | Set activity/class type metadata; show class copy on detail | `flow-create-game` |
| P3 | Create form | Header **Create Game** + footer **Publish game** in class mode | Class-specific title/CTA when `createMode === 'class'` | Copy polish (tier 1 P3) |
| P3 | Marcus dependency | Parent enroll share only demonstrable on seeded demo classes | Document v1 gap; builder must not rely on Marcus for validator | B6 cross-ref |

**Tier 2 silent-failure check:** Publish/create **does not** silent-fail — network create succeeds. Failure is **wrong journey outcome** (pickup vs class), not a dead button.

## Sport-specific

- Created on **badminton** court; persona goal is class enrollment UX, not sport seed.

## Contract checklist (tier 2)

| Item | Pass | Notes |
|------|------|-------|
| Approved coach reaches Create Class | **Yes** | |
| Class create publishes coach class | **No** | Pickup activity only |
| Parent enrollment invite shareable | **No** on `playerr0474532`; **Yes** on Marcus demo | |
| No silent failure on publish | **Yes** | |
| Non-Marcus coach end-to-end | **No** | Demo data masks share UX |

## Screenshots

| File | State |
|------|-------|
| `2026-06-22/01-profile-coach-tools-create-class.png` | Coach Tools + Create Class (`playerr0474532`) |
| `2026-06-22/02-create-class-class-clinic-banner.png` | Create form — Class/Clinic banner |
| `2026-06-22/03-post-create-pickup-game-detail.png` | Post-publish — Pickup game + Copy link |
| `2026-06-22/04-today-classes-i-teach-empty-after-create.png` | Today (capture attempted) |
| `2026-06-22/05-class-detail-share-parent-enrollment-invite.png` | Marcus class detail — **Share parent enrollment invite** (reference) |
| `2026-06-22/05-marcus-class-roster-reference.png` | Marcus roster with enrolled students (reference) |

## Recommended contract changes

- [ ] `flow-create-game`: Split **coach class publish** from **pickup publish**; checklist row for parent enroll share (not `shareGameInvite`).
- [ ] `flow-student-class-enrollment`: Document that coach must reach `ClassDetail` — blocked for non-Marcus until class listing persistence ships.
- [ ] `flow-coach-onboarding-org`: **CLASSES I TEACH** must reflect coach-created classes, not demo-only rows.
