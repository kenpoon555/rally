# Product review — theme-reviewer · 2026-06-26

**Queue:** theme-explore-round1 · **Persona:** `theme-reviewer` (art director) · **Tier 6 (taste) + Tier 3 lens (a11y/contrast)**
**Deliverable:** themed-screenshot matrix (baseline + 2 pitches × 3 Join-Loop screens) + per-theme rationale.
**Renders:** `docs/product-review/theme-reviewer/2026-06-26/` · **Matrix doc:** [theme-rounds/2026-06-26/theme-matrix.md](../../redesign/theme-rounds/2026-06-26/theme-matrix.md)

## Method note (deviation from script)
Step 1 (Metro + sim baseline capture) was **blocked**: the installed `com.rallyapp` is a standalone build with no embedded JS bundle and no Metro connection — sim shows `No script URL provided. unsanitizedScriptURLString = (null)`. Same blocker as Tier-6 validation. Per the theme plan ("generated renders are mockups"), I produced all candidates — including baseline — as generated mockups with **layout/spacing/type held constant** and **palette-only** variation. Live capture is deferred to the post-pick `theme.ts` swap.

## Candidates pitched
| Tag | Name | Direction |
|-----|------|-----------|
| A | Court Neon (baseline/control) | current lime `#B1E248` / yellow `#FCFD59` on light gray |
| B | Clean Sport | blue `#2F6BFF` on white, orange `#FF7A1A` reserved for live/now |
| C | Night Court | dark mode, lime accent on near-black `#0E120F` |

Screens: 01 Discover feed · 02 Game detail (join banner + grouped roster) · 03 Post-game → next. Screens 4–5 from the plan (commit state, game room) are palette-equivalent state-variants of 02 and ride its read.

## Friction / findings table (taste + a11y)

| # | Sev | Screen | Theme(s) | Finding | Contract impact |
|---|-----|--------|----------|---------|-----------------|
| T1 | P0 | Discover + detail | A, C | **Lime primary CTA vs lime "You're in" pill collide** — two lime elements compete; the join CTA loses primacy. | `module-visual-design-system`: enforce one filled-primary lime per card; status chips use tint not solid. |
| T2 | P1 | All | A | Lime fills require **dark `onPrimary`** to pass WCAG; white text on lime fails. Already our rule — flag any drift. | `module-visual-design-system` contrast gate (already gated). |
| T3 | P1 | All | C | Dark mode needs a **full token pass** (dividers, status colors, disabled states) — not a palette swap. Treat as separate workstream. | Out of scope for a palette round; note in plan. |
| T4 | info | All | B | **Two-signal system reads cleanly**: blue = action, orange = time pressure. Strongest hierarchy + easiest contrast pass. | Take B to validator contrast gate. |
| T5 | info | Discover | B, C | Urgency hook ("4 spots · starts in 3h") legibility good in B (orange) and C (lime); weak in A (lime-on-white small text). | `module-game-card`: urgency hook min contrast. |

## Per-theme verdict
- **B — Clean Sport → ship-candidate (iterate).** Best action hierarchy, accessibility headroom, closest to the reference the founder liked. **Recommend taking to validator gate.**
- **A — Court Neon → keep as brand option.** Strong identity; must resolve T1 (lime CTA vs lime chip) before it can win.
- **C — Night Court → drop this round / revisit as dark mode.** Great evening-pickup vibe but it's a dark-mode project, not a palette swap (T3).

## Contract impact summary
- `module-visual-design-system` (focus): add explicit "one filled-primary per surface" + reaffirm `onPrimary`/`onAccent` contrast; theming exploration gate already present.
- `module-game-card`: urgency-hook minimum contrast note.
- No src changes proposed (palette decision pending founder pick).

## Human decision needed
Pick a direction (B recommended) or request a fresh pitch to replace C. On pick → validator runs the contrast gate **before** any `theme.ts` change.
