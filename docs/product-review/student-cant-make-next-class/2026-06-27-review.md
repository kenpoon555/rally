# Product review — student-cant-make-next-class · 2026-06-27

**Queue:** class-response-round1 · **Persona:** `student-cant-make-next-class` (R3/R5 parent-managed) · **Tier 1**
**Contract:** [flow-class-session-response.md](../../contracts/flow-class-session-response.md)
**Method:** static code audit on `dev` @ post-merge (PR #90/#91). Sim not run — feature absent in UI.

## Journey attempted
Today → **My Classes** card (`TodayMyClassesCard`) → mark upcoming session **Can't make it** in ≤2 taps → see status flip.

## Verdict
**FAIL** — contract not implemented. Data model exists; affordance missing.

## Friction table

| # | Sev | Screen | Finding | Contract row |
|---|-----|--------|---------|--------------|
| C1 | **P0** | TodayMyClassesCard | Row is tap-only → navigates to ClassDetail. **No Confirm / Can't make it actions** when `response_status === 'not_responded'`. | Next Class card §inline actions |
| C2 | **P0** | TodayMyClassesCard | No write path to `response_status` from UI — grep shows display-only in this component. | Can't make it → `cant_make_it` |
| C3 | **P0** | TodayMyClassesCard | No optimistic update / undo after response. | optimistic + undo |
| C4 | P1 | TodayMyClassesCard | Cancelled/deferred sessions show correct **label** but actions N/A (not rendered either way). | disable actions on dead session |
| C5 | info | ClassDetailScreen | Coach `groupedRoster` already groups by status — will work once student write exists. | coach visibility |

## Contract impact
- `flow-class-session-response`: all checklist rows **fail** until builder implements TodayMyClassesCard actions + service write.
- No contract text change needed — spec is correct; implementation gap only.

## Screenshots
Deferred — no interactive surface to capture until C1–C2 ship.
