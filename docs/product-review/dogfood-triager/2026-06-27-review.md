# Product review — dogfood-triager · 2026-06-27

## Persona
Lens: **Founder build-truth** (Tier 0, catalog I) · Build: `fix/class-response-builder` (Metro hot-reload) on **Android `emulator-5554`** · iOS on stale-session onboarding (see note).
Goal: Use the **real** just-built Join-Loop + class-response build end-to-end and, for every observation, classify it (`makes-sense` / `necessary` / `bug` / `needs-more-detail` / `simplify`) and route it. **Not** a happy-path coverage pass — a skeptical "is this actually right?" walk.

## What worked
- Discover → detail pull-through is fast and obvious; one clear primary action per surface.
- "Starts in 9h" urgency hook reads as a clean "why now" without competing with the spot badge.
- Non-member detail now ends in a single, unambiguous **Request to Join** CTA.

## Friction (prioritized)

| P | Screen | Issue | Suggested change | Contract impact |
|---|--------|-------|------------------|-----------------|
| **P1** | Discover card (`GameListCard`) | Same card showed **"5 left"** (badge) and **"9 spots left"** (urgency hook) — two contradicting numbers | Badge = single spot source; urgency hook = time-to-start only | **Fixed.** `module-game-card` "Derived counts — single source of truth" added |
| **P1** | Game detail (`ActivityDetailScreen`) | **Non-member** saw **"Open Game Room"** (member surface) + "Request to Join" | Gate Game Room entry on membership, not `canOpenActivityChat` liveness | **Fixed.** `flow-game-room` viewer-state × visible-actions matrix added |
| **P2** | Game detail | **"1 of 1 marked ready"** while WHO'S GOING = **3** and 3 green ready dots show | Ready count must read same roster as WHO'S GOING / dots | New — `flow-rally-session` ready-count semantics |
| **P2** | Discover vs detail | Discover badge "5 left" vs detail "7 spots open" same game — server min-to-start vs live open-to-capacity, both labeled "spots" | Distinct labels per meaning (e.g. "5 to start" vs "7 open") | New — resolve under `module-game-card` cross-surface rule |
| **P3** | Today (`HomeScreen`) | Header subtitle "… · **4:50 PM**" vs Next Up card "Wed · **5:00 PM**" time mismatch | Single time source for header + card | New — `flow-today-home` (TBD) |
| — | Today / class card | `TodayMyClassesCard` (class-response) not reachable on this player login — needs parent w/ enrollment | Re-run on `marcus@…` parent account | Deferred — covered by class-response-round1 code audit |

## State coverage (state-matrix lens, partial)
Game-detail **not-joined** viewer state verified live: Request to Join only, no Open Game Room (screenshot 02). Member/host/pending/finalized states code-audited via the `isGameMember` gate; live verification deferred (single-login emulator, iOS blocked). Full state-matrix walk is persona 2/2 (`state-matrix-skeptic`).

## Recommended contract changes
- [x] `module-game-card` — derived counts single source of truth (done this cycle)
- [x] `flow-game-room` — viewer-state × visible-actions matrix (done this cycle)
- [ ] `flow-rally-session` — define ready-count source (WHO'S GOING vs "N of M marked ready" vs dots)
- [ ] `module-game-card` — concrete label resolution for discover "left" vs detail "open"
- [ ] `flow-today-home` (TBD) — single source for Next Up time vs header subtitle

## Evidence
- `2026-06-27/01-discover-card-counts.png` — "5 left" + "Starts in 9h" (contradiction resolved)
- `2026-06-27/02-detail-nonmember-cta.png` — non-member: Request to Join only

## iOS note
iOS sim (`iPhone 17 Pro`) showed black/splash through the 8–12s bootstrap, then onboarding — stale Supabase session, invalid refresh token (`AuthApiError`), auto-recovered by `clearStaleBootstrapSession`. Not a code regression; re-sign-in clears. Triage ran on Android.
