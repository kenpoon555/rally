/**
 * Product surfaces gated until Rally has live partnerships and density.
 * Flip individual flags when ready — no env wiring required for beta.
 */

/** Coaches/clinics carousel, partner court filter, intro nights, sport landing promo on Play. */
export const PLAY_PARTNER_SURFACES_ENABLED = false;

/** Captain apply, concierge manual match, captain feedback — off for App Store production (Guideline 2.2). */
export const BETA_OPS_SURFACES_ENABLED = false;
